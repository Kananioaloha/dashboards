---
title: "Star Wars Dashboard"
author: "Kanani TimSing"
format: dashboard
---

```{r, echo=false}
library(tidyverse)
library(plotly)

```

## Row 1

```{r, warning = false}
ggplot(data = starwars, aes(x = mass, y = height)) +
  geom_point()
```

## Row 2

```{r}
ggplot(data = starwars, aes(x = mass, y = height)) +
  geom_point()
```

```{r}
ggplot(data = starwars, aes(x = mass, y = height)) +
  geom_point()
```
